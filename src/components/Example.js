import React from 'react';
import { withRouter } from 'dva/router';

const Example = () => {
  return (
    <div>
      Example
    </div>
  );
};

Example.propTypes = {
};

export default withRouter(Example);
